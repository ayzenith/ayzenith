import type {Metadata} from "next";import Link from "next/link";import {getOsSettings} from "@/server/os/settings";import {getCashflow,materialiseRecurring} from "@/server/os/finance";import {cashflowReport} from "@/server/os/reports";import {CASHFLOW_BUCKETS} from "@/config/os";import {Card,Money,PageHead,Table,Td,Th,Tr,btn} from "@/components/os/ui";
export const metadata:Metadata={title:"Finans · Business OS"};export const dynamic="force-dynamic";export default async function Finance(){const s=await getOsSettings();await materialiseRecurring(12);const [c,r]=await Promise.all([getCashflow(s.baseCurrency),cashflowReport({})]);return <><PageHead title="Finans kokpiti" actions={<><Link className={btn.secondary} href="/os/payments?overdue=1">Gecikmişler</Link><a className={btn.secondary} href="/os/report-pdf/finance-cashflow/pdf" target="_blank" rel="noreferrer">PDF</a><Link className={btn.primary} href="/os/payments?new=IN">Tahsilat ekle</Link></>}/><Card title="Nakit akışı takvimi" padded={false}><Table><thead><tr><Th>Vade</Th><Th align="right">Girecek</Th><Th align="right">Çıkacak</Th><Th align="right">Net</Th></tr></thead><tbody>{CASHFLOW_BUCKETS.map(b=><Tr key={b.key}><Td>{b.label}</Td><Td align="right"><Money value={c.incoming[b.key]} currency={s.baseCurrency}/></Td><Td align="right"><Money value={c.outgoing[b.key]} currency={s.baseCurrency}/></Td><Td align="right"><Money value={c.incoming[b.key]-c.outgoing[b.key]} currency={s.baseCurrency} tone="auto"/></Td></Tr>)}</tbody></Table></Card><Card className="mt-4" title="Aylık görünüm" padded={false}><Table><tbody>{r.map(x=><Tr key={x.month}><Td>{x.month}</Td><Td align="right"><Money value={x.net} currency={s.baseCurrency}/></Td></Tr>)}</tbody></Table></Card></>}
